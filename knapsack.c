#include<stdio.h>
int main(){
int max(int a,int b);
int k,int wt[],int val[],int pt[];
int i,j;
for(i=0;i<k;i++)
{
    for(j=0;j<=i;j++)
    {
        if(i==0||w=0)
        {
            k[w][i]=0;
        }
        else if(wt[i-1]<=w)
            k[i][w]=max(val)-max(wt)
    }
    return 0;
}
}
